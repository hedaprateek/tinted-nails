# Tinted Nails

Marketing site for **Tinted Nails**, a hand-painted press-on nail studio.

Single-file static site — `index.html` holds the markup, styles and script. No build step, no dependencies. The only external request is Google Fonts (Bodoni Moda, Jost, DM Mono).

## What's on the page

- **Live shade card** — tapping any of the ten shades re-tints the whole page: the nails in the tray, the gallery designs, the buttons, the step numbers, the wordmark dot.
- **Drawn, not photographed** — the five nail shapes and all eight gallery finishes (reverse French, chrome, cat-eye, florals, checkerboard, ombré, glitter, line work) are generated as inline SVG, so every shade renders instantly and nothing depends on stock imagery.
- Sections: shades, products, services, gallery, shapes & lengths, application steps, reviews, Instagram, wear & care, ordering.
- Light and dark themes, responsive down to phone widths, respects `prefers-reduced-motion`.

## Making changes

Phone number, Instagram handle, hours, shipping line, reviews and gallery photos all live in one
`SITE` / `GALLERY` block near the bottom of `index.html` — search the file for `EDIT THIS BLOCK`.
**[EDITING.md](EDITING.md)** walks through it step by step, including uploading photos from the
GitHub website with no tools installed.

## Still to replace

- **WhatsApp number** — `#order` links to `wa.me/910000000000`.
- **Instagram handle** — `@tintednails` in the `#instagram` block; both Instagram buttons link to bare `instagram.com`.
- **Reviews** — the three quotes in `#reviews` are placeholders and are labelled as such on the page. Swap in real customer messages and delete the `.sample` chip and the note beside it.
- **Gallery photos** — see below.

## Editing

Everything lives in `index.html`:

| What | Where |
| --- | --- |
| Shade names, hex values, finishes | the `SHADES` array in the `<script>` at the bottom |
| Gallery finishes and their captions | the `DESIGNS` array, drawn by the `art()` function below it |
| Colours, fonts, spacing | the `:root` token block at the top of `<style>` |
| Products and prices | the `#products` section |
| Services and turnarounds | the `#services` section |
| WhatsApp number and Instagram handle | the `#order` and `#instagram` blocks |

### Swapping a drawn tile for a real photo

The gallery is generated by `paintGallery()`. To use photographs instead, drop the files in a `photos/`
folder and replace the generated markup for that tile with:

```html
<figure class="tile">
  <div class="tile-art"><img src="photos/chrome-mirror.jpg" alt="Chrome mirror set on coffin nails"></div>
  <figcaption><span class="t-name">Chrome Mirror</span><span class="t-meta">Coffin · long</span></figcaption>
</figure>
```

`.tile-art img` is already styled to fill the tile. Square or 4:5 portrait crops work best.

## Running locally

Open `index.html` in a browser. That's it.

## Deploying

Pushing to `main` publishes to GitHub Pages automatically.
